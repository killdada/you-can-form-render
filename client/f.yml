service: form-render-demo

provider:
  name: aliyun

deployType:
  type: static
  version: 3.0.0

package:
  include:
    - build
  exclude:
    - package-lock.json
    - yarn.lock

custom:
  customDomain:
    domainName: auto
